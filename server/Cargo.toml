[package]
name = "tish-playground-server"
version = "0.1.0"
edition = "2021"
description = "Static file server for Tish playground (local dev)"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["rt-multi-thread", "net"] }
tower-http = { version = "0.5", features = ["fs"] }
